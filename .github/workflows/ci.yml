name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '10.0.x'
    
    - name: Restore dependencies
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore --configuration Release

    - name: Diagnose PhysicsEngineAdvancedTests
      run: dotnet test tests/Brine2D.Tests --no-build --configuration Release --filter "FullyQualifiedName=Brine2D.Tests.Systems.Physics.PhysicsEngineAdvancedTests.GetSleepingBodies_SettledDynamicBody_AppearsInList" --verbosity normal
      
    - name: Test
      run: dotnet test tests/Brine2D.Tests --no-build --configuration Release --verbosity normal --collect:"XPlat Code Coverage"
    
    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v5
      with:
        token: ${{ secrets.CODECOV_TOKEN }}